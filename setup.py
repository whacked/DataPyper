from setuptools import setup

setup(name="DataPyper",
        version = "0.0.1",
        description = "nipype interface for csv and pandas data structures",
        packages = ["DataPyper",],
        )
